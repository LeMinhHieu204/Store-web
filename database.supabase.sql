DROP TABLE IF EXISTS deposit_transactions CASCADE;
DROP TABLE IF EXISTS order_items CASCADE;
DROP TABLE IF EXISTS orders CASCADE;
DROP TABLE IF EXISTS product_images CASCADE;
DROP TABLE IF EXISTS products CASCADE;
DROP TABLE IF EXISTS posts CASCADE;
DROP TABLE IF EXISTS contacts CASCADE;
DROP TABLE IF EXISTS categories CASCADE;
DROP TABLE IF EXISTS users CASCADE;

CREATE TABLE users (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    role VARCHAR(20) NOT NULL DEFAULT 'user',
    balance NUMERIC(15, 2) NOT NULL DEFAULT 0,
    remember_token VARCHAR(100),
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT users_role_check CHECK (role IN ('user', 'admin'))
);

CREATE TABLE categories (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    slug VARCHAR(255) NOT NULL UNIQUE,
    image_path VARCHAR(255),
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE products (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    slug VARCHAR(255) NOT NULL UNIQUE,
    description TEXT NOT NULL,
    original_price NUMERIC(12, 2),
    price NUMERIC(12, 2) NOT NULL DEFAULT 0,
    thumbnail VARCHAR(255) NOT NULL,
    file_path VARCHAR(255) NOT NULL,
    category_id BIGINT NOT NULL REFERENCES categories(id) ON DELETE CASCADE,
    is_featured BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE product_images (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    product_id BIGINT NOT NULL REFERENCES products(id) ON DELETE CASCADE,
    image_path VARCHAR(255) NOT NULL,
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE orders (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    total_price NUMERIC(12, 2) NOT NULL DEFAULT 0,
    status VARCHAR(20) NOT NULL DEFAULT 'pending',
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT orders_status_check CHECK (status IN ('pending', 'paid', 'completed', 'cancelled'))
);

CREATE TABLE order_items (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    order_id BIGINT NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
    product_id BIGINT NOT NULL REFERENCES products(id) ON DELETE CASCADE,
    price NUMERIC(12, 2) NOT NULL DEFAULT 0,
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE posts (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    slug VARCHAR(255) NOT NULL UNIQUE,
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE contacts (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    message TEXT NOT NULL,
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE deposit_transactions (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    code VARCHAR(255) NOT NULL UNIQUE,
    amount NUMERIC(15, 2) NOT NULL,
    status VARCHAR(50) NOT NULL DEFAULT 'pending',
    bank_name VARCHAR(255),
    bank_account_name VARCHAR(255),
    bank_account_number VARCHAR(255),
    payment_reference VARCHAR(255) UNIQUE,
    paid_at TIMESTAMP NULL,
    meta JSONB,
    created_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO users (id, name, email, password, role, balance, created_at, updated_at) VALUES
(1, 'admin', 'admin@example.com', 'admin123', 'admin', 0, NOW(), NOW()),
(2, 'user', 'user@example.com', 'user123', 'user', 0, NOW(), NOW());

INSERT INTO categories (id, name, slug, image_path, created_at, updated_at) VALUES
(1, 'Laravel', 'laravel', 'uploads/categories/laravel.jpg', NOW(), NOW()),
(2, 'WordPress', 'wordpress', 'uploads/categories/wordpress.jpg', NOW(), NOW()),
(3, 'React', 'react', 'uploads/categories/react.jpg', NOW(), NOW());

INSERT INTO products (id, title, slug, description, original_price, price, thumbnail, file_path, category_id, is_featured, created_at, updated_at) VALUES
(1, 'Marketplace Laravel Pro', 'marketplace-laravel-pro', 'Bo source code ban hang so voi dashboard, thanh toan va phan quyen.', 1090000, 890000, 'uploads/products/laravel-pro.jpg', 'uploads/products/marketplace-laravel-pro.zip', 1, TRUE, NOW(), NOW()),
(2, 'Blog WordPress Premium', 'blog-wordpress-premium', 'Giao dien blog chuan SEO, de tuy bien, toi uu toc do tai.', 590000, 490000, 'uploads/products/wp-blog.jpg', 'uploads/products/blog-wordpress-premium.zip', 2, FALSE, NOW(), NOW()),
(3, 'Admin Panel React', 'admin-panel-react', 'Template React admin panel dung cho dashboard SaaS hien dai.', 790000, 690000, 'uploads/products/react-admin.jpg', 'uploads/products/admin-panel-react.zip', 3, TRUE, NOW(), NOW());

INSERT INTO product_images (id, product_id, image_path, created_at, updated_at) VALUES
(1, 1, 'uploads/products/gallery/laravel-pro-1.jpg', NOW(), NOW()),
(2, 1, 'uploads/products/gallery/laravel-pro-2.jpg', NOW(), NOW()),
(3, 2, 'uploads/products/gallery/wp-blog-1.jpg', NOW(), NOW()),
(4, 3, 'uploads/products/gallery/react-admin-1.jpg', NOW(), NOW());

INSERT INTO orders (id, user_id, total_price, status, created_at, updated_at) VALUES
(1, 2, 890000, 'completed', NOW(), NOW()),
(2, 2, 490000, 'paid', NOW(), NOW());

INSERT INTO order_items (id, order_id, product_id, price, created_at, updated_at) VALUES
(1, 1, 1, 890000, NOW(), NOW()),
(2, 2, 2, 490000, NOW(), NOW());

INSERT INTO posts (id, title, content, slug, created_at, updated_at) VALUES
(1, 'Kinh nghiem chon source code ban hang', 'Bai viet chia se cach chon source code phu hop voi mo hinh kinh doanh.', 'kinh-nghiem-chon-source-code-ban-hang', NOW(), NOW()),
(2, 'Cac buoc trien khai source code tren Laragon', 'Huong dan cau hinh Apache, MySQL va virtual host tren Laragon.', 'cac-buoc-trien-khai-source-code-tren-laragon', NOW(), NOW());

INSERT INTO contacts (id, name, email, message, created_at, updated_at) VALUES
(1, 'Le Minh', 'minh@example.com', 'Minh can tu van source code cho website ban khoa hoc.', NOW(), NOW()),
(2, 'Tran An', 'an@example.com', 'Cho minh hoi cach cap nhat phien ban moi cua source.', NOW(), NOW());

SELECT setval(pg_get_serial_sequence('users', 'id'), COALESCE((SELECT MAX(id) FROM users), 1), TRUE);
SELECT setval(pg_get_serial_sequence('categories', 'id'), COALESCE((SELECT MAX(id) FROM categories), 1), TRUE);
SELECT setval(pg_get_serial_sequence('products', 'id'), COALESCE((SELECT MAX(id) FROM products), 1), TRUE);
SELECT setval(pg_get_serial_sequence('product_images', 'id'), COALESCE((SELECT MAX(id) FROM product_images), 1), TRUE);
SELECT setval(pg_get_serial_sequence('orders', 'id'), COALESCE((SELECT MAX(id) FROM orders), 1), TRUE);
SELECT setval(pg_get_serial_sequence('order_items', 'id'), COALESCE((SELECT MAX(id) FROM order_items), 1), TRUE);
SELECT setval(pg_get_serial_sequence('posts', 'id'), COALESCE((SELECT MAX(id) FROM posts), 1), TRUE);
SELECT setval(pg_get_serial_sequence('contacts', 'id'), COALESCE((SELECT MAX(id) FROM contacts), 1), TRUE);
SELECT setval(pg_get_serial_sequence('deposit_transactions', 'id'), COALESCE((SELECT MAX(id) FROM deposit_transactions), 1), TRUE);
